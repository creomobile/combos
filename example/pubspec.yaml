name: combos_example
description: Example App for 'combos' package

version: 1.0.0

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  english_words: ^3.1.5
  combos: any
  editors: ^0.0.9
  demo_items:
    git:
      url: https://github.com/creomobile/demo_items

dependency_overrides:
  combos:
    path: ../

dev_dependencies:
  pedantic: ^1.8.0

flutter:
  uses-material-design: true
